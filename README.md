# Metamaterial Works Website

## Add content

The content of the website is located in the `src` directory. The directory is split into into directories each representing a locale. The default locale is English (`en`).
